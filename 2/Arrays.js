"use strict";
{
    let arr = []; //wszystkie wartosci tablicy sa typem number
    arr.push(34);
    const touple = ["Pawel", 32]; // kolejne typy na poszczeolnych miejscach tablicy w tym przykladzie mozemy do tablicy przypisac tylko dwie wartosci gdzie pierwsza to string a druga number (tylko podczas tworzenia)
    touple.push("as"); //ale mozemy dalej pushowac o ile sa to zmienne okreslone wczesniej
    touple.push("323");
}
