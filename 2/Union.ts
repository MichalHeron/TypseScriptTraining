{
	let ux: string | number //zmienna ux jest typem string lub number oddzielona pipe operatorem

	ux = 5
	ux = 'x'

	let uz: string | number | boolean

	const f = (x: string | boolean): number | string => {
		return ''
	}
}
